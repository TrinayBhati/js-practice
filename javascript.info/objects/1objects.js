	// key value pair
	// let user = new Object(); object construct we can declare an object like this
	let user  ={ // an object
		name : "John", // key is the name and value is John
		age =30, //  key is agre and value is 30

	};
	alert(user.name); // John
	let key = prompt("What would you like to know about user","");
	alert(user[key]);
