	
	//let func = (arg1 , arg2) =>expression;
	let sum =(a,b) => a+b;
	// returns a+b when called sum
	let double = n => n*2;
	// returns double
	let sayHi =() => alert("Hi");
	//alerts Hi
	// we can use {} as well instead of single statement of work but there we need explicit return statement
	let sum = (a,b) =>{
		return (a+b);
	}